{% extends  'layout/base.html.twig' %}

{% block title %}Build Your Form{% endblock %}

{% block body %}
   <div class="card">
      <div class="card-header">Créer un formulaire</div>
      <div class="card-body">
          {{ form_start(form) }}
          {{ form_end(form) }}
         <button type="button" class="btn btn-primary">Primary</button>
      </div>
   </div>
{% endblock %}
