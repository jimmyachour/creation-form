{% extends  'layout/base.html.twig' %}

{% block title %}<title>Build Your Form</title>{% endblock %}

{% block body %}
   <div class="card">
      <div class="card-header">Créer un formulaire</div>
      <div class="card-body">
          {{ form_start(form) }}
          {{ form_end(form) }}
      </div>
   </div>
{% endblock %}