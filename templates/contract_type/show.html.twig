{% extends  'layout/base.html.twig' %}

{% block title %}Contracts Type Show{% endblock %}



{% block body %}
    <h1>ContractType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contract_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ contract_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contract_type_index') }}">back to list</a>

    <a href="{{ path('contract_type_edit', {'id': contract_type.id}) }}">edit</a>

    {{ include('contract_type/_delete_form.html.twig') }}
{% endblock %}
